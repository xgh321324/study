#coding:utf-8from selenium import webdriverimport time,rebrowser = webdriver.Chrome()browser.get('https://www.baidu.com/')browser.maximize_window()#获取class属性的值c = browser.find_element_by_id('kw').get_attribute('class')print(c)#在百度搜索输入框内输入值后，那么这里输入的值也是可以获取到的#browser.find_element_by_id('kw').send_keys('南京天气')#value = browser.find_element_by_id('kw').get_attribute('value')#print(value)'''有时候通过元素的属性的查找页面上的某个元素，可能不太好找，这时候可以从源码中爬出想要的信息。selenium 的 page_source 方法可以获取到页面源码'''page = browser.page_source#print('页面源码：',page)## "非贪婪匹配,re.S('.'匹配字符,包括换行符)"url_list = re.findall('href=\"(.*?)\"', page, re.S)url_all = []for url in url_list:    if 'http' in url:        url_all.append(url)    else:        pass#最终的url集合print(url_all)browser.close()browser.quit()